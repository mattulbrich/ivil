# Automatically created on Wed Mar 20 09:49:01 CET 2013
include "../dij/dij.decl.p"
include "bfs.array.algo.p"

problem [0; bfs_array]true

(*

Instantiaation:

(\argmin a_w; a_w2<=a_w & a_w<a_size & seqGet(a_succ(a_v), a_w); a_w)

*)
