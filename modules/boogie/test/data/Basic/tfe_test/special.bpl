/**
  This example is used to test special characters.
  It might also be a perl script.
*/

type ';
const #:';
function $('):';
procedure #<^>($:^) returns (~:^)
{
  var \:';
  assert $(#) == $ || ~ == $ || $ == $ || $(\) != \;
}
