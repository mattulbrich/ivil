package de.uka.iti.pseudo.gui;

/**
 * this interface merely lists those constants which are used to denote states
 * within the gui.
 */
public interface StateConstants {

    /**
     * this is used to trigger that we are within an automatic proof
     */
    String IN_PROOF = "in proof";
    
    /**
     * this is used to trigger that a window does not contain a proof
     * but only very rudimentary menus.
     */
    String VIRGIN_WINDOW = "virgin window";
    
}
