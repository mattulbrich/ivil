include "BFS.minDistance(int,int).p"

properties
   CompoundStrategy.strategies 
      "SimplificationStrategy,
       BreakpointStrategy,
       KnowledgeStrategy,
       SMTStrategy"

problem [0; Java]true 